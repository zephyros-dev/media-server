name: Renovate branch test
on:
  pull_request:
    branches:
      - "renovate**"
  merge_group:
jobs:
  renovate-branh:
    name: Make test auto pass to limit the concurrent PR from renovate
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pass"
