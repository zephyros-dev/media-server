- name: Setup server
  no_log: true
  hosts: server
  tasks:
    - name: Setup server
      ansible.builtin.import_role:
        name: managed_node
    - name: Setup container
      ansible.builtin.import_role:
        name: container
