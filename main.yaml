- name: Setup server
  no_log: "{{ lookup('ansible.builtin.env', 'ANSIBLE_NO_LOG') | default(false)}}"
  hosts: server
  tasks:
    - name: Setup server
      ansible.builtin.import_role:
        name: managed_node
    - name: Setup container
      ansible.builtin.import_role:
        name: container
