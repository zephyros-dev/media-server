- name: Prepare data folder
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
  loop:
    - "{{ global_media }}"
    - "{{ radarr_hq_web_config }}"
    - "{{ transmission_download }}"

- name: Stop systemd
  ansible.builtin.systemd:
    name: librespeed
    daemon_reload: true
    enabled: true
    scope: user
    state: stopped
  ignore_errors: true

- name: Pod
  containers.podman.podman_pod:
    infra_name: radarr-hq-pod
    name: radarr-hq
    network: caddy
    state: "{{ radarr_state }}"

- name: Radarr-hq
  containers.podman.podman_container:
    env:
      PGID: "{{ global_pgid }}"
      PUID: "{{ global_puid }}"
      TZ: "{{ global_timezone }}"
    image: "{{ radarr_image }}"
    labels:
      io.containers.autoupdate: registry
    name: radarr-hq-web
    pod: radarr-hq
    restart_policy: on-failure
    state: "{{ radarr_state }}"
    volumes:
      - "{{ global_media }}:/home"
      - "{{ radarr_hq_web_config }}:/config:U,z"
      - "{{ transmission_download }}:/downloads"
  register: container
  until: container.failed == false

- name: Install systemd
  vars:
    service_name: radarr-hq
    service_state: "{{ radarr_state }}"
  import_tasks: systemd.yaml
