- import_tasks: network.yaml
- import_tasks: caddy.yaml
  tags: caddy
# - import_tasks: diun.yaml
# - import_tasks: grafana.yaml
# - import_tasks: librenms.yaml
# - import_tasks: pgadmin.yaml

# - import_tasks: archivy.yaml
#   tags: archivy

# - import_tasks: gitea.yaml
# - import_tasks: headphones.yaml
# - import_tasks: heimdall.yaml
- import_tasks: mumble.yaml
  tags: mumble
- import_tasks: samba.yaml
  tags: samba
- import_tasks: scrutiny.yaml
  tags: scrutiny
- import_tasks: sftp.yaml
# - import_tasks: sftpgo.yaml
# - import_tasks: sonarr.yaml
- import_tasks: transmission.yaml
  tags: transmission

- name: Run podman container with systemd
  include_tasks: systemd_container.yaml
  args:
    apply:
      tags:
        - "{{ systemd_container_loop.key }}"
  loop: "{{ systemd_container_service | dict2items }}"
  loop_control:
    loop_var: systemd_container_loop
  vars:
    systemd_container_loop_var: "{{ systemd_container_loop }}"
  tags:
    - always
