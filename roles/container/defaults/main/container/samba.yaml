# samba_image: elswork/samba:latest
samba_image: docker.io/servercontainers/samba:smbd-only-latest
samba_state: started
# samba_password:
samba_shares_settings: valid users = root; guest ok = no; read only = no; browseable = yes;
