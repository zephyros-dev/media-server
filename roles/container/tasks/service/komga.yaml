- name: Komga
  containers.podman.podman_container:
    env:
      TZ: "{{ global_timezone }}"
    image: "{{ komga_image }}"
    name: komga-web
    pod: komga
    state: "{{ komga_state }}"
    volumes:
      - "{{ global_media }}:/home"
      - "{{ komga_volume_config }}:/config:U,z"
  register: container
  until: container.failed == false
