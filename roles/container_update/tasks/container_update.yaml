- name: Import container_run role
  import_role:
    name: container_run

- name: Update images
  containers.podman.podman_image:
    name: "{{ item }}"
    force: yes
  loop: "{{ global_container_auto_update }}"
