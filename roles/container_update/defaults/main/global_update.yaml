# List of image to be autoupdate
global_container_auto_update:
  - "{{ archivy_web_image }}"
  - "{{ archivy_elasticsearch_image }}"

  - "{{ bazarr_image }}"

  - "{{ caddy_image }}"

  - "{{ calibrew_image }}"

  - "{{ diun_image }}"

  - "{{ gitea_web_image }}"
  - "{{ gitea_db_image }}"

  - "{{ headphones_image }}"

  - "{{ heimdall_image }}"

  - "{{ jackett_image }}"

  - "{{ jellyfin_image }}"

  - "{{ librenms_web_image }}"
  - "{{ librenms_db_image }}"

  - "{{ librespeed_image }}"

  - "{{ lidarr_image }}"

  - "{{ mumble_image }}"

  - "{{ nextcloud_web_image }}"
  - "{{ nextcloud_redis_image }}"
  - "{{ nextcloud_postgres_image }}"

  - "{{ nocodb_web_image }}"
  - "{{ nocodb_db_image }}"

  - "{{ pgadmin_image }}"

  - "{{ privatebin_image }}"

  - "{{ pymedusa_image }}"

  - "{{ radarr_image }}"

  # - '{{ readarr_image }}'

  - "{{ scrutiny_image }}"

  # - '{{ sonarr_image }}'

  - "{{ syncthing_image }}"

  - "{{ transmission_image }}"
