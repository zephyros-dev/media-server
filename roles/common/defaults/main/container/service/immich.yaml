immich_volume: "{{ global_volume_path }}/immich"
immich_volume_database: "{{ immich_volume }}/database"
immich_volume_upload: "{{ immich_volume }}/upload"
immich_volume_ml_cache: "{{ immich_volume }}/ml_cache"
# immich_database_password:
# immich_jwt_secret:
