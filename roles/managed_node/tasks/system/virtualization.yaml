- name: Install the 'virtualization' package group
  become: yes
  dnf:
    name: "@virtualization"
    state: latest
# TODO: Error: The `lxml` module is not importable. Check the requirements
# - name: Create default network for libvirt
#   become: yes
#   community.libvirt.virt_net:
#     autostart: yes
#     command: create
#     name: default
