- name: Install the 'virtualization' package group
  become: True
  dnf:
    name: "@virtualization"
    state: latest

- name: Create default network for libvirt
  become: True
  community.libvirt.virt_net:
    autostart: yes
    command: create
    name: default
